#! /bin/bash  
varvalues=../assets/scripts/values
cd ./../../compilation_debug/
status=0
while read line 
do 
	status=$?
	echo $status
	if [ $status -eq 0 ] ; then
		echo -e  "$line" 
		./projet $line
	fi

done < $varvalues
